{
  "name": "ng-workshop",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "concurrently": "3.1.0"
  },
  "scripts": {
    "start": "concurrently -r --names \"ClIENT|SERVER\" \"npm run --prefix server start\" \"npm run --prefix client start\"",
    "install": "cd server && npm install && cd ../client && npm install",
    "uninstall": "cd server && npm uninstall && cd ../client && npm uninstall"
  }
}
