import { Component, Input, Output } from '@angular/core';
import { EventEmitter } from 'events';

@Component({
  selector: 'app-tag-filter',
  template: ``
})
export class TagFilterStubComponent {
  @Input() products;
}
